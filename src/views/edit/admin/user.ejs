<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="utf-8" />
      <meta
         name="viewport"
         content="width=device-width initial-scale=1 shrink-to-fit=no"
         />
      <meta name="description" content="data-driven full-stack web-application" />
      <title>View User</title>
      <!-- Bootstrap core CSS -->
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
      <!-- ajax -->
      <script src="https://code.jquery.com/jquery-3.6.0.min.js" defer> </script>
      <!-- Update user js -->
      <script src="/js/admin/update/user.js" defer> </script>

   </head>
   <body>
      <%- include('../../common/header'); -%>
      <div class="container">
         <div class="row justify-content-md-center">
            <div class="col-">
               <form id="admin-update-user">
                  <div class="form-group">
                     <label id="name-label" for="name">Name</label>
                     <input
                        value="<%= user.name %>"
                        type="text"
                        class="form-control"
                        name="name"
                        />
                  </div>
                  <div class="form-group">
                     <label id="email-label" for="name">Email</label>
                     <input
                        value="<%= user.email %>"
                        type="text"
                        class="form-control"
                        name="email"
                        />
                  </div>
                  <div class="form-group">
                     <label id="admin-label" for="name">Administrator</label>
                     <input
                        value="<%= user.isAdmin%>"
                        type="text"
                        class="form-control"
                        name="isAdmin"
                        />
                  </div>
                  <div class="form-group">
                     <label id="verified-label" for="verified">Verified</label>
                     <input
                        value="<%= user.verified %>"
                        type="text"
                        class="form-control"
                        name="isVerified"
                        />
                  </div>
                  <input type="hidden" name="user_id" value="<%= user.user_id%>">
                  <button type="submit" class="btn btn-primary">Submit</button>
                  <tr>
                    <td>
                        <a  href="/admin/users/" class="btn btn-dark btn-xs"><span class="glyphicon glyphicon-remove"></span> Go back</a>
                    </td>
                </tr>
               </form>
            </div>
         </div>
      </div>
      <%- include('../../common/footer'); -%>
   </body>
</html>