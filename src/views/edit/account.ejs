<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="utf-8" />
      <meta
         name="viewport"
         content="width=device-width initial-scale=1 shrink-to-fit=no"
         />
      <meta name="description" content="data-driven full-stack web-application" />
      <title>Edit Account</title>
      <!-- Bootstrap core CSS -->
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
      <!-- ajax -->
      <script src="https://code.jquery.com/jquery-3.6.0.min.js" defer> </script>
      <!-- Edit Account js -->
      <script src="/js/edit/account.js" defer> </script>
   </head>
   <body>
      <%- include('../common/header'); -%>
      <div class="container">
         <div class="row justify-content-md-center">
            <div class="col-">
               <form id="edit-account" action="/accounts/edit" method="POST">
                  <div class="form-group">
                     <label id="name-label" for="name">Account Name</label>
                     <input
                        value="<%= account.name %>"
                        type="text"
                        class="form-control"
                        id="name"
                        name="name"
                        placeholder="Enter Name"
                        />
                  </div>
                  <div class="form-group">
                     <label id="accountno-label" for="name">Account Number</label>
                     <input
                        value="<%= account.accountno %>"
                        type="text"
                        class="form-control"
                        id="accountno"
                        name="accountno"
                        />
                  </div>
                  <div class="form-group">
                     <label id="sortcode-label" for="name">Account Sort Code</label>
                     <input
                        value="<%= account.sortcode%>"
                        type="text"
                        class="form-control"
                        id="sortcode"
                        name="sortcode"
                        />
                  </div>
                  <div class="form-group">
                     <label id="balance-label" for="balance">Account Balance</label>
                     <input
                        value="<%= account.balance %>"
                        type="text"
                        class="form-control"
                        id="balance"
                        name="balance"
                        />
                  </div>
                  <input type="hidden" name="account_id" value="<%= account.account_id %>">
                  <button type="submit" class="btn btn-primary">Submit</button>
                  <tr>
                     <td>
                        <a  href="/accounts/view/<%= account.account_id%>" class="btn btn-dark btn-xs"><span class="glyphicon glyphicon-remove"></span> Go back</a>
                     </td>
                  </tr>
               </form>
            </div>
         </div>
      </div>
      <%- include('../common/footer'); -%>
   </body>
</html>