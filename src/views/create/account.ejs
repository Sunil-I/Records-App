<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="utf-8" />
      <meta
         name="viewport"
         content="width=device-width, initial-scale=1, shrink-to-fit=no"
         />
      <meta name="description" content="data-driven full-stack web-application" />
      <title>Create Account</title>
      <!-- Bootstrap core CSS -->
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css" media="print" onload="this.media='all'; this.onload=null;">
      <!-- ajax -->
      <script src="https://code.jquery.com/jquery-3.6.0.min.js" defer> </script>
      <!-- Create Account js -->
      <script src="/js/createAccount.js" defer> </script>
   </head>
   <body>
      <%- include('../common/header'); -%>
      <div class="container">
         <h2 class="text-center">Create A New Account</h2><br>
         <div class="row justify-content-md-center">
            <div class="col-">
               <form id="new-account" method="POST" action="/accounts/new">
                  <div class="form-group">
                     <label id="name-label">Account Name</label>
                     <input
                        type="text"
                        class="form-control"
                        id="name"
                        name="name"
                        placeholder="Enter Name"
                        required
                        />
                  </div>
                  <div class="form-group">
                     <label id="accountno-label">Account Number</label>
                     <input
                        type="text"
                        class="form-control"
                        id="accountno"
                        name="accountno"
                        placeholder="735452509"
                        required
                        />
                  </div>
                  <div class="form-group">
                    <label id="sortcode-label">Account Sort code</label>
                    <input
                       type="text"
                       class="form-control"
                       id="sortcode"
                       name="sortcode"
                       placeholder="25827297"
                       required
                       />
                 </div>
                 <div class="form-group">
                  <label id="balance-label">Account Balance</label>
                  <input
                     type="text"
                     class="form-control"
                     id="balance"
                     name="balance"
                     placeholder="0"
                     required
                     />
               </div>
                  <button type="submit" class="btn btn-primary">Submit</button>
               </form>
            </div>
         </div>
      </div>
      <%- include('../common/footer'); -%>
   </body>
</html>