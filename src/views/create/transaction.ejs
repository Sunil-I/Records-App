<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="utf-8" />
      <meta
         name="viewport"
         content="width=device-width, initial-scale=1, shrink-to-fit=no"
         />
      <meta name="description" content="data-driven full-stack web-application" />
      <title>Create Transaction</title>
      <!-- Bootstrap core CSS -->
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css" media="print" onload="this.media='all'; this.onload=null;">
      <!-- ajax -->
      <script src="https://code.jquery.com/jquery-3.6.0.min.js" defer> </script>
      <!-- Create Account js -->
      <script src="/js/createTransaction.js" defer> </script>
   </head>
   <body>
      <%- include('../common/header'); -%>
      <div class="container">
         <h2 class="text-center">Create A New Transaction</h2><br>
         <div class="row justify-content-md-center">
            <div class="col-">
               <form id="new-transaction" method="POST" action="/transactions/new">
                  <div class="form-group">
                     <label id="id-label">Account ID</label>
                     <input
                        type="text"
                        class="form-control"
                        id="id"
                        name="id"
                        placeholder="Enter Account ID"
                        required
                        />
                  </div>
                  <div class="form-group">
                     <label id="type-label">Transaction Type</label>
                     <input
                        type="text"
                        class="form-control"
                        id="type"
                        name="type"
                        placeholder="withdrawl or deposit"
                        required
                        />
                  </div>
                 <div class="form-group">
                  <label id="amount-label">Amount</label>
                  <input
                     type="text"
                     class="form-control"
                     id="amount"
                     name="amount"
                     placeholder="0"
                     required
                     />
               </div>
                  <button type="submit" class="btn btn-primary">Submit</button>
               </form>
            </div>
         </div>
      </div>
      <%- include('../common/footer'); -%>
   </body>
</html>