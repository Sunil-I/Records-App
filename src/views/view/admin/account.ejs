<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="utf-8" />
      <meta
         name="viewport"
         content="width=device-width initial-scale=1 shrink-to-fit=no"
         />
      <meta name="description" content="data-driven full-stack web-application" />
      <title>View Account</title>
      <!-- Bootstrap core CSS -->
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
       
   </head>
   <body>
      <%- include('../../common/header'); -%>
      <div class="container">
         <div class="row justify-content-md-center">
            <div class="col-">
               <form>
                <div class="form-group">
                    <label for="account id">Account ID</label>
                    <input
                       value="<%= account.account_id %>"
                       type="text"
                       class="form-control"
                       readonly
                       />
                 </div>
                  <div class="form-group">
                     <label for="account name">Account Name</label>
                     <input
                        value="<%= account.name %>"
                        type="text"
                        class="form-control"
                        readonly
                        />
                  </div>
                  <div class="form-group">
                     <label for="account number">Account Number</label>
                     <input
                        value="<%= account.accountno %>"
                        type="text"
                        class="form-control"
                        readonly
                        />
                  </div>
                  <div class="form-group">
                     <label for="account sort code">Account Sort Code</label>
                     <input
                        value="<%= account.sortcode%>"
                        type="text"
                        class="form-control"
                        readonly
                        />
                  </div>
                  <div class="form-group">
                     <label for="balance">Account Balance</label>
                     <input
                        value="<%= account.balance %>"
                        type="text"
                        class="form-control"
                        readonly
                        />
                  </div>
                  <div class="form-group">
                    <label id="createdat-label" for="account created at">Account CreatedAt</label>
                    <input
                       value="<%= account.createdAt.toLocaleDateString('en-GB') %>"
                       type="text"
                       class="form-control"
                       readonly
                       />
                 </div>
                 <div class="form-group">
                    <label id="last-label" for="account last updated">Account Last Updated</label>
                    <input
                       value="<%= account.updatedAt.toLocaleDateString('en-GB') %>"
                       type="text"
                       class="form-control"
                       readonly
                       />
                 </div>
                  <tr>
                    <td>
                        <a  href="/admin/accounts/" class="btn btn-primary btn-xs"><span class="glyphicon glyphicon-remove"></span> Go back</a>
                    </td>
                    <td>
                        <a  href="/admin/accounts/edit/<%= account.account_id%>" class="btn btn-dark btn-xs"><span class="glyphicon glyphicon-remove"></span> Edit Account</a>
                    </td>
                </tr>
               </form>
            </div>
         </div>
      </div>
      <%- include('../../common/footer'); -%>
   </body>
</html>